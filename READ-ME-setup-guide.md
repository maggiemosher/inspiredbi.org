# Wolfpack's Guide to AI â€” Setup Guide ## Files in this pack | File | What it is | Goes live at | |---|---|---| | **what-is-ai.html** | The "What is AI" page (Wolfpack's Guide â€” 5 concepts, videos, staff/student guidance, contact form) | `inspiredbi.org/what-is-ai.html` | | **professional-development.html** | Sign-in / routing gateway (links to all areas, first = What is AI) | `inspiredbi.org/professional-development.html` | | **integration-snippets.html** | 3 copy-paste blocks (top link, tutorials box, homepage PD section) | pasted into existing pages | --- ## STEP 1 â€” Where is your site hosted? Your site loads through **Cloudflare** (confirmed â€” Cloudflare's email-protection and analytics scripts run on your pages). So you are **NOT** updating raw GitHub Pages anymore. One of two setups: - **Cloudflare Pages + connected GitHub repo** â†’ you STILL push files to **GitHub**; Cloudflare auto-deploys in ~1 min. - **Cloudflare Pages + Direct Upload** â†’ you upload files in the **Cloudflare dashboard**; GitHub is not involved. **How to tell which (30 seconds):** Cloudflare dashboard â†’ **Workers & Pages** â†’ your `inspiredbi` project â†’ **Settings â†’ Build & deployments**. - If it shows a **connected GitHub repo** â†’ use **Path A**. - If it says **Direct Upload / "Upload assets"** â†’ use **Path B**. --- ## STEP 2 â€” Publish the 3 new files ### Path A â€” GitHub-connected 1. Open your repo â†’ add **what-is-ai.html**, **professional-development.html** to the **root** (same folder as `index.html`). 2. Commit â†’ Cloudflare rebuilds automatically. ### Path B â€” Cloudflare Direct Upload 1. Cloudflare â†’ **Workers & Pages** â†’ your project â†’ **Create deployment** (or **Upload assets**). 2. Drag in **what-is-ai.html** and **professional-development.html** alongside your other files â†’ **Deploy**. > Put both files in the **top level** (root), so the links `what-is-ai.html` and `professional-development.html` resolve. --- ## STEP 3 â€” Paste the 3 snippets (from integration-snippets.html) | Snippet | Paste into | Where exactly | |---|---|---| | **1 â€” Top link** | `index.html` (and any page you want) | right after `
` | | **2 â€” Tutorials box** | `tutorials.html` | as the **first card** in your tutorials grid | | **3 â€” Professional Development** | `index.html` | at the **bottom**, just before your footer | Re-publish the edited pages the same way as Step 2. --- ## Your access code **`WOLVES1837`** â€” the Wolves + **1837**, the year Cheyney became America's first HBCU. Short, meaningful, memorable. - Signs people in on the gateway â†’ routes them to **What is AI**. - Unlocks the **resource attachments** on the What is AI page. - **Aliases** that also work: `WOLFPACK`. General educators: `INSPIRED` â†’ Free Continued Learning. - **To change it:** edit `const ACCESS_CODE = "WOLVES1837";` in what-is-ai.html, and the `ROUTES` table in professional-development.html. > âš ï¸ **Important â€” this is a light gate, not real security.** It keeps casual visitors out, but anyone who views page source can see the code. Do **not** put truly sensitive files behind it. For real protection, host the attachment PDFs behind Cloudflare Access (free for small teams) or a password-protected area. --- ## Two things to finish the resource pack & contact form 1. **Resource attachments** on What is AI currently point to `#`. Replace each `href="#"` with the real PDF path once you upload the decks/cheat-sheet/lesson plan (e.g., `presentations/wolfpack-ai-basics.pdf`). 2. **Contact form** opens the visitor's email app to `contactinspiredbi@gmail.com` (works now, zero setup). To have submissions land straight in an inbox instead, create a free **Formspree** form and follow the one-line swap noted in the form's HTML comment. --- ## Quick check after publishing - Visit `inspiredbi.org/what-is-ai.html` â†’ all **5 videos** play, nav links jump correctly. - Visit `inspiredbi.org/professional-development.html` â†’ enter **WOLVES1837** â†’ lands on What is AI. - Homepage bottom **Professional Development** button â†’ opens the gateway. 
